import React from 'react';
import type { Meta, StoryObj } from '@storybook/react';
import { {{itemName}} } from './{{itemKebab}}';

const meta: Meta<typeof {{itemName}}> = {
  title: 'Components/{{itemName}}',
  component: {{itemName}},
  parameters: {
    layout: 'centered',
  },
  tags: ['autodocs'],
  argTypes: {
    // Add your prop controls here
  },
};

export default meta;

type Story = StoryObj<typeof {{itemName}}>;

export const Default: Story = {
  args: {},
};
