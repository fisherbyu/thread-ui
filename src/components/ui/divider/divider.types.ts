export type DividerProps = {
	/** Width override. Defaults to `75%` */
	width?: string;
	/** Vertical margin override. Defaults to `16px` */
	marginY?: string;
	/** Line thickness @default `'standard'` */
	weight?: 'light' | 'standard' | 'heavy';
};
