export type SkeletonProps = {
	/** Width of the skeleton block @default '100%' */
	w?: string;
	/** Height of the skeleton block @default '1rem' */
	h?: string;
	/** Whether to render as a circle (e.g. avatars) @default false */
	round?: boolean;
};
