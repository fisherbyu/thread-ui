import { generateStyles } from '../../utils/styling/generate-styles';
import { LayoutBoxProps } from './layout-box.types';

export const LayoutBox = ({
	children,
	display,
	visibility,
	overflow,
	overflowX,
	overflowY,
	opacity,
	order,
	flexGrow,
	flexShrink,
	flexBasis,
	alignSelf,
	position,
	top,
	right,
	bottom,
	left,
	zIndex,
	height,
	width,
	minHeight,
	minWidth,
	maxHeight,
	maxWidth,
	padding,
	paddingBottom,
	paddingLeft,
	paddingRight,
	paddingTop,
	paddingX,
	paddingY,
	margin,
	marginBottom,
	marginLeft,
	marginRight,
	marginTop,
	marginX,
	marginY,
}: LayoutBoxProps) => {
	const styles = generateStyles({
		visibility,
		overflow,
		overflowX,
		overflowY,
		opacity,
		order,
		flexGrow,
		flexShrink,
		flexBasis,
		alignSelf,
		position,
		top,
		right,
		bottom,
		left,
		zIndex,
		height,
		width,
		minHeight,
		minWidth,
		maxHeight,
		maxWidth,
		padding,
		paddingBottom,
		paddingLeft,
		paddingRight,
		paddingTop,
		paddingX,
		paddingY,
		margin,
		marginBottom,
		marginLeft,
		marginRight,
		marginTop,
		marginX,
		marginY,
	});
	styles.display = display;
	return <div style={styles}>{children}</div>;
};
